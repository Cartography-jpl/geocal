if($?PYTHONPATH) then
  setenv PYTHONPATH @pythondir@:@platpythondir@:${PYTHONPATH}
else
  setenv PYTHONPATH @pythondir@:@platpythondir@
endif
if ($?PATH) then
  setenv PATH @prefix@/bin:${PATH}
else
  setenv PATH @prefix@/bin
endif
