This supplies the python GeoCal library.

Note that the python is code is tested by "installcheck". This is because
we need to install the python module before we can test it. You can also
run the python check directly as "make python_check".

