# This file was automatically generated by SWIG (https://www.swig.org).
# Version 4.1.1
#
# Do not make changes to this file unless you know what you are doing - modify
# the SWIG interface file instead.

from sys import version_info as _swig_python_version_info
from ._swig_wrap import _pan_sharpen

try:
    import builtins as __builtin__
except ImportError:
    import __builtin__

_swig_new_instance_method = _pan_sharpen.SWIG_PyInstanceMethod_New
_swig_new_static_method = _pan_sharpen.SWIG_PyStaticMethod_New

def _swig_repr(self):
    try:
        strthis = "proxy of " + self.this.__repr__()
    except __builtin__.Exception:
        strthis = ""
    return "<%s.%s; %s >" % (self.__class__.__module__, self.__class__.__name__, strthis,)


def _swig_setattr_nondynamic_instance_variable(set):
    def set_instance_attr(self, name, value):
        if name == "this":
            set(self, name, value)
        elif name == "thisown":
            self.this.own(value)
        elif hasattr(self, name) and isinstance(getattr(type(self), name), property):
            set(self, name, value)
        else:
            raise AttributeError("You cannot add instance attributes to %s" % self)
    return set_instance_attr


def _swig_setattr_nondynamic_class_variable(set):
    def set_class_attr(cls, name, value):
        if hasattr(cls, name) and not isinstance(getattr(cls, name), property):
            set(cls, name, value)
        else:
            raise AttributeError("You cannot add class attributes to %s" % cls)
    return set_class_attr


def _swig_add_metaclass(metaclass):
    """Class decorator for adding a metaclass to a SWIG wrapped class - a slimmed down version of six.add_metaclass"""
    def wrapper(cls):
        return metaclass(cls.__name__, cls.__bases__, cls.__dict__.copy())
    return wrapper


class _SwigNonDynamicMeta(type):
    """Meta class to enforce nondynamic attributes (no new attributes) for a class"""
    __setattr__ = _swig_setattr_nondynamic_class_variable(type.__setattr__)


import weakref

SWIG_MODULE_ALREADY_DONE = _pan_sharpen.SWIG_MODULE_ALREADY_DONE
class SwigPyIterator(object):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")

    def __init__(self, *args, **kwargs):
        raise AttributeError("No constructor defined - class is abstract")
    __repr__ = _swig_repr
    __swig_destroy__ = _pan_sharpen.delete_SwigPyIterator
    value = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_value)
    incr = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_incr)
    decr = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_decr)
    distance = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_distance)
    equal = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_equal)
    copy = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_copy)
    next = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_next)
    __next__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___next__)
    previous = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_previous)
    advance = _swig_new_instance_method(_pan_sharpen.SwigPyIterator_advance)
    __eq__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___eq__)
    __ne__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___ne__)
    __iadd__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___iadd__)
    __isub__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___isub__)
    __add__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___add__)
    __sub__ = _swig_new_instance_method(_pan_sharpen.SwigPyIterator___sub__)
    def __iter__(self):
        return self

# Register SwigPyIterator in _pan_sharpen:
_pan_sharpen.SwigPyIterator_swigregister(SwigPyIterator)
SHARED_PTR_DISOWN = _pan_sharpen.SHARED_PTR_DISOWN

import os

def _new_from_init(cls, version, *args):
    '''For use with pickle, covers common case where we just store the
    arguments needed to create an object. See for example HdfFile'''
    if(cls.pickle_format_version() != version):
      raise RuntimeException("Class is expecting a pickled object with version number %d, but we found %d" % (cls.pickle_format_version(), version))
    inst = cls.__new__(cls)
    inst.__init__(*args)
    return inst

def _new_from_serialization(data):
    return geocal_swig.serialize_function.serialize_read_binary(data)

def _new_from_serialization_dir(dir, data):
    curdir = os.getcwd()
    try:
      os.chdir(dir)
      return geocal_swig.serialize_function.serialize_read_binary(data)
    finally:
      os.chdir(curdir)


def _new_vector(cls, version, lst):
    '''Create a vector from a list.'''
    if(cls.pickle_format_version() != version):
      raise RuntimeException("Class is expecting a pickled object with version number %d, but we found %d" % (cls.pickle_format_version(), version))
    inst = cls.__new__(cls)
    inst.__init__()
    for i in lst:
       inst.append(i)
    return inst

def _new_from_set(cls, version, *args):
    '''For use with pickle, covers common case where we use a set function 
    to assign the value'''
    if(cls.pickle_format_version() != version):
      raise RuntimeException("Class is expecting a pickled object with version number %d, but we found %d" % (cls.pickle_format_version(), version))
    inst = cls.__new__(cls)
    inst.__init__()
    inst.set(*args)
    return inst

import geocal_swig.calc_raster_multi_band
import geocal_swig.raster_image_multi_band_variable
import geocal_swig.raster_image_multi_band
import geocal_swig.generic_object
import geocal_swig.with_parameter
import geocal_swig.geocal_exception
class PanSharpen(geocal_swig.calc_raster_multi_band.CalcRasterMultiBand):
    r"""

    This create a set of pan sharpened images from a pan band and a set of
    multispectral bands (at lower resolution).

    This uses the pan sharpening algorithm described in "WorldView-2 Pan-
    Sharpening" by Chris Padwick et. al. (ASPRS 2010 Annual Conference).

    Because of the way this is calculated, it is most efficient to
    calculate all bands for a given tile at one time. So you should access
    all bands for a particular tile before going to the next tile for this
    object. Nothing bad happens if you don't, it is just slower to access.

    C++ includes: pan_sharpen.h 
    """

    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")
    __repr__ = _swig_repr

    def __init__(self, Pan, Mul, Force_rpc=False, Log_progress=True):
        r"""

        PanSharpen::PanSharpen(const boost::shared_ptr< RasterImage > &Pan, const boost::shared_ptr<
        RasterImageMultiBand > &Mul, bool Force_rpc=false, bool
        Log_progress=true)
        Constructor.

        This takes in a pan image and a multi spectral image. We subset to the
        area we can calculate PanSharpen over.

        Parameters:
        -----------

        Pan:  The panchromatic image to use

        Mul:  The multispectral images to use

        Force_rpc:  Sometimes an image will have both map information and an
        RPC. In this case, we use the map information by preference, unless
        directed by Force_rpc being true.

        Log_progress:  If true, write progress message to std::cout as we work
        through the data. 
        """
        _pan_sharpen.PanSharpen_swiginit(self, _pan_sharpen.new_PanSharpen(Pan, Mul, Force_rpc, Log_progress))
    __swig_destroy__ = _pan_sharpen.delete_PanSharpen

# Register PanSharpen in _pan_sharpen:
_pan_sharpen.PanSharpen_swigregister(PanSharpen)

__all__ = ["PanSharpen"]


